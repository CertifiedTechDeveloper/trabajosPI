// Tipo String (cadena de caracteres): se escribe entre comillas simples o dobles
let nombre='Ana';

// Tipo Number: numero entero o decimal y NaN (not a number)
 let edad = 25;

 // Tipo Boolean: true o false
  let llueve: true;
  let esta_soleado : false;

  // Tipo Undefined: valor sin definir
   let edad;

   // Tipo Null: valor nulo(vacio o desconocido)
   let nulo = null;

   // Tipo Not a number(NaN): no es un numero en si, pero tampoco es un string ni un booleano, ej: ‘32’/2 =NaN
    let notNumber = '32'/2;

    // Tipo Objeto literal: nos permite sumar mucha informacion en una variable

    let TiposAutos = {
        marca: 'Bmw',
        modelo: 'tiguan',
        anio: 2017,
        funciona: true
           }

   /* TipoArray (lista o arreglo): nos permite agrupar varios tipos de datos en una variable. 
   No tiene claves, tiene indices numericos que comienzan en el numero cero*/
   let seriesFavoritas = [
        'friends', 'bigBan', 'got'
   ]



